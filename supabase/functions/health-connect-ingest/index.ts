import { createClient } from "@supabase/supabase-js";
const headers={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json"};
const respond=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers});
function normalizedActivityType(a:any){const raw=String(a?.activity_type||'other').toLowerCase();if(raw!=='other')return raw;const title=String(a?.title||'').toLowerCase();if(/\b(bici|bicicleta|cycling|ciclismo|bike)\b/.test(title))return'cycling';if(/f[uú]tbol|football|soccer/.test(title))return'football';if(/carrera|running|run\b/.test(title))return'run';if(/fuerza|pesas|strength|weight/.test(title))return'gym';return'other'}
const richFields=['duration_min','moving_time_min','distance_km','avg_hr','max_hr','calories','top_speed_kmh','high_intensity_m','sprint_count','absolute_sprint_count','avg_pace_sec_km','elevation_gain_m'];
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response('ok',{headers});if(req.method!=='POST')return respond({error:'Method not allowed'},405);const auth=req.headers.get('Authorization');if(!auth)return respond({error:'Missing authorization'},401);const supabase=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}});const token=auth.replace(/^Bearer\s+/i,''),{data:userData,error:userError}=await supabase.auth.getUser(token);if(userError||!userData.user)return respond({error:'Unauthorized'},401);const userId=userData.user.id;let body:any;try{body=await req.json()}catch{return respond({error:'Invalid JSON'},400)}if(!body||typeof body!=='object'||!Array.isArray(body.sleep)||!Array.isArray(body.activities))return respond({error:'sleep and activities arrays are required'},400);if(body.sleep.length>50||body.activities.length>100)return respond({error:'Batch too large; send at most 50 sleeps and 100 activities'},400);const sleeps=body.sleep,activities=body.activities,failures:Array<{kind:string,index:number,code:string}>=[];let sleepCount=0,activityCount=0;
 for(let i=0;i<sleeps.length;i++){const s=sleeps[i];if(!s?.sleep_date){failures.push({kind:'sleep',index:i,code:'invalid_date'});continue}const row={user_id:userId,sleep_date:s.sleep_date,source:'health_connect',sleep_start:s.sleep_start||null,sleep_end:s.sleep_end||null,total_sleep_min:s.total_sleep_min??null,deep_sleep_min:s.deep_sleep_min??null,rem_sleep_min:s.rem_sleep_min??null,light_sleep_min:s.light_sleep_min??null,awake_min:s.awake_min??null,awake_count:s.awake_count??null,sleep_score:s.sleep_score??null,avg_hrv:s.avg_hrv??null,hrv_baseline_low:s.hrv_baseline_low??null,hrv_baseline_high:s.hrv_baseline_high??null,resting_hr:s.resting_hr??null,naps:Array.isArray(s.naps)?s.naps:[],metrics:{...(s.metrics||{}),external_id:s.external_id||null,source_app:s.source_app||null}};const {error}=await supabase.from('sleep_records').upsert(row,{onConflict:'user_id,sleep_date,source'});if(!error)sleepCount++;else failures.push({kind:'sleep',index:i,code:error.code||'write_failed'})}
 for(let i=0;i<activities.length;i++){const a=activities[i];if(!a?.external_id||!a?.activity_date||!a?.activity_type){failures.push({kind:'activity',index:i,code:'missing_fields'});continue}const {data:existing}=await supabase.from('activities').select('id,activity_type,title,duration_min,moving_time_min,distance_km,avg_hr,max_hr,calories,top_speed_kmh,high_intensity_m,sprint_count,absolute_sprint_count,avg_pace_sec_km,elevation_gain_m,metrics').eq('user_id',userId).eq('source','health_connect').eq('external_id',String(a.external_id)).maybeSingle();const enriched=!!existing?.metrics?.fit_enriched,normalizedType=normalizedActivityType(a);const hcSnapshot={activity_type:normalizedType,title:a.title||null,duration_min:a.duration_min??null,distance_km:a.distance_km??null,avg_hr:a.avg_hr??null,max_hr:a.max_hr??null,calories:a.calories??null,source_app:a.source_app||null,exercise_type:a.metrics?.health_connect_exercise_type??null,synced_at:new Date().toISOString()};const row:any={user_id:userId,activity_date:a.activity_date,started_at:a.started_at||null,activity_type:enriched?existing.activity_type:normalizedType,source:'health_connect',external_id:String(a.external_id),title:enriched?(existing.title||a.title||null):(a.title||null),metrics:enriched?{...(existing.metrics||{}),health_connect_snapshot:hcSnapshot,source_app:a.source_app||existing.metrics?.source_app||null}:{...(a.metrics||{}),route_points:Array.isArray(a.route_points)?a.route_points.slice(0,2500):[],source_app:a.source_app||null}};for(const field of richFields)row[field]=enriched?(existing?.[field]??a?.[field]??null):(a?.[field]??null);const {error}=await supabase.from('activities').upsert(row,{onConflict:'user_id,source,external_id'});if(!error)activityCount++;else failures.push({kind:'activity',index:i,code:error.code||'write_failed'})}
 const {error:syncError}=await supabase.from('sync_sources').upsert({user_id:userId,source:'health_connect',status:failures.length?'error':'connected',...(!failures.length?{last_sync_at:new Date().toISOString()}:{}),details:{sleep:sleepCount,activities:activityCount,failures}},{onConflict:'user_id,source'});if(syncError)failures.push({kind:'sync',index:0,code:syncError.code||'write_failed'});return respond({ok:failures.length===0,sleep:sleepCount,activities:activityCount,failures,...(failures.length?{error:'Sincronización incompleta. Reintenta; los registros importados no se duplicarán.'}:{})},failures.length?422:200)});
