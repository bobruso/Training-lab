const BUILD='v9.9 · build coros101';
let installed=false,retries=0;
function applyBuild(){const meta=document.querySelector('meta[name="build"]');if(meta)meta.content=BUILD;const footer=document.getElementById('buildVersion');if(footer)footer.textContent=BUILD;if(window.TrainingLab?.state&&window.TrainingLab.state.frontend!==BUILD)window.TrainingLab.state.frontend=BUILD}
function installSettings(){if(installed)return;const sync=document.getElementById('settingsHealthSync');if(!sync){if(retries++<50)setTimeout(installSettings,200);return}installed=true;const actions=sync.closest('.actions');if(actions&&!actions.querySelector('[data-hc-settings]')){const btn=document.createElement('button');btn.type='button';btn.className='btn alt';btn.dataset.hcSettings='1';btn.textContent='Permisos avanzados / GPS';btn.onclick=()=>{if(window.TrainingLabAndroid?.openHealthConnectSettings)window.TrainingLabAndroid.openHealthConnectSettings();else alert('Abre Health Connect → Permisos de aplicaciones → Training Lab y permite los datos deportivos disponibles.');};actions.append(btn)}const card=sync.closest('.card');if(card&&!card.querySelector('[data-hc-rich-note]')){const p=document.createElement('p');p.className='small muted';p.dataset.hcRichNote='1';p.textContent='La APK 0.9 puede leer ruta GPS, velocidad, desnivel, cadencia y potencia cuando COROS los publica y Health Connect concede acceso.';card.append(p)}}
function init(){applyBuild();installSettings()}
if(typeof document!=='undefined'){if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()}
