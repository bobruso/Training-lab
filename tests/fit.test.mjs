import test from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import {readFileSync} from 'node:fs';import {stripTypeScriptTypes} from 'node:module';import FitParser from 'fit-file-parser';import {analyzeFootballSession} from '../supabase/functions/analyze-fit/analysis/football.js';import {analyzeRunningSession} from '../supabase/functions/analyze-fit/analysis/running.js';import {analyzeCyclingSession} from '../supabase/functions/analyze-fit/analysis/cycling.js';import {buildBaseFitAnalysis} from '../supabase/functions/analyze-fit/analysis/base-analysis.js';import {makeReport} from '../supabase/functions/analyze-fit/analysis/generic-report.js';import {num,normalizeCoord,episodeCount,textValue,ts} from '../supabase/functions/analyze-fit/analysis/edge-common.js';import {analyzeStrengthSession,normalizeStrengthSets} from '../supabase/functions/analyze-fit/analysis/strength.js';import {persistFitAnalysis} from '../supabase/functions/analyze-fit/analysis/persist-analysis.js';
function fixture(){const def=Buffer.from([0x40,0,0,20,0,4,253,4,0x86,5,4,0x86,6,2,0x84,3,1,2]),records=[];for(let i=0;i<=1200;i++){const r=Buffer.alloc(12);r[0]=0;r.writeUInt32LE(1157846400+i,1);r.writeUInt32LE(i*500,5);r.writeUInt16LE(5000,9);r[11]=150;records.push(r);}const data=Buffer.concat([def,...records]),header=Buffer.alloc(14);header[0]=14;header[1]=0x20;header.writeUInt16LE(2100,2);header.writeUInt32LE(data.length,4);header.write('.FIT',8);const crc=b=>{let c=0;for(const x of b){c^=x;for(let j=0;j<8;j++)c=(c&1)?(c>>>1)^0xA001:c>>>1;}return c;};header.writeUInt16LE(crc(header.subarray(0,12)),12);const content=Buffer.concat([header,data]),tail=Buffer.alloc(2);tail.writeUInt16LE(crc(content));return Buffer.concat([content,tail]);}
function edge({path='owner/test.fit',type='football',blob=fixture()}={}){let handler;const writes=[];const client={auth:{getUser:async()=>({data:{user:{id:'owner'}}})},storage:{from:()=>({download:async()=>({data:new Blob([blob])})})},from:table=>{const q={select(){return q;},eq(){return q;},single:async()=>({data:table==='fit_files'?{id:'fit',user_id:'owner',activity_id:'activity',storage_path:path}:{id:'activity',activity_type:type}}),maybeSingle:async()=>({data:{hr_max_bpm:190}}),update(row){writes.push({table,row});return q;},delete(){writes.push({table,delete:true});return q;},insert(row){writes.push({table,row});return q;},upsert(row){writes.push({table,row});return q;},then(resolve){return Promise.resolve({error:null}).then(resolve)}};return q;}};const source=readFileSync('supabase/functions/analyze-fit/index.ts','utf8').replace(/^import .*;\r?\n/gm,'').replace('export default handler;','');const context={Response,Request,TextDecoder,createClient:()=>client,FitParser,analyzeFootballSession,analyzeRunningSession,analyzeCyclingSession,analyzeStrengthSession,buildBaseFitAnalysis,makeReport,num,textValue,ts,normalizeStrengthSets,persistFitAnalysis,Deno:{env:{get:()=>''},serve:fn=>handler=fn}};vm.runInNewContext(stripTypeScriptTypes(source),context);return{handler,writes}}
const req=()=>new Request('http://local',{method:'POST',headers:{Authorization:'Bearer test'},body:JSON.stringify({fit_file_id:'fit'})});
test('real synthetic FIT binary parses through the full handler with finite metrics',async()=>{const {handler,writes}=edge();const response=await handler(req()),data=await response.json();assert.equal(response.status,200,JSON.stringify(data));assert.equal(data.summary.distanceKm,6);assert.equal(data.summary.durationSec,1200);assert.equal(data.summary.avgHr,150);assert.equal(data.summary.first10MinM,3000);assert.equal(data.summary.last10MinM,3000);assert.equal(data.summary.absoluteSprintCount,1);assert.ok(writes.some(x=>x.table==='activity_analysis'));assert.ok(writes.some(x=>x.row?.parse_status==='parsed'));});
test('FIT rejects another user storage path and corrupt bytes',async()=>{assert.equal((await edge({path:'another/file.fit'}).handler(req())).status,403);assert.equal((await edge({blob:Buffer.from('not a FIT')}).handler(req())).status,422);});
test('FIT strength without set messages does not invent sets',async()=>{const {handler,writes}=edge({type:'gym'}),r=await handler(req()),body=await r.json();assert.equal(r.status,200);assert.equal(body.summary.strengthSetCount,0);assert.equal(body.summary.strengthAnalysisVersion,'strength-v1');assert.ok(!writes.some(x=>x.table==='strength_sets'&&Array.isArray(x.row)));});
test('FIT missing values remain null and recording gaps split sprints',()=>{assert.equal(num(null),null);assert.equal(normalizeCoord(undefined),null);assert.equal(episodeCount([{t:0,s:20},{t:1000,s:20},{t:60000,s:20},{t:61000,s:20}],18),0);});
